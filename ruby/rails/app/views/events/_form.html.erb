<%= form_with(:model => [page_user, event]) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <th><%= form.label :description %></th>
      <td><%= form.text_field :description %></td>
    </tr>
    <tr>
      <th><%= form.label :date %></th>
      <td><%= form.date_select :date, :start_year => Date.today.year, :end_year => Date.today.year + 1 %></td>
    </tr>
    <tr>
      <th><%= form.label :recurring %></th>
      <td><%= form.check_box :recur %></td>
    </tr>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
