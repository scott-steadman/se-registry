$('#friend__list').append('<%= j render :partial => 'friend', :collection => @friends %>');

<% if @friends.next_page %>
$(App.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @friends %>');
<% else %>
$(window).off('scroll');
$(App.nextPageLoader.scrollerSelector).remove();
<% end %>
