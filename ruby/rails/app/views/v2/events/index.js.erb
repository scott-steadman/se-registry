$('#event__list').append('<%= j render :partial => 'event', :collection => @events %>');

<% if @events.next_page %>
$(App.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @events %>');
<% else %>
$(window).off('scroll');
$(App.nextPageLoader.scrollerSelector).remove();
<% end %>

