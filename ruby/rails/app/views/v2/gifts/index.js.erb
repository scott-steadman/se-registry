$('#gift__list').append('<%= j render :partial => 'gift', :collection => @gifts %>');

<% if @gifts.next_page %>
$(window.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @gifts %>');
<% else %>
$(window).off('scroll');
$(window.nextPageLoader.scrollerSelector).remove();
<% end %>
