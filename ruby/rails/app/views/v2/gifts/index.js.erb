$('#gift__list').append('<%= j render :partial => 'gift', :collection => @gifts %>');

<% if @gifts.next_page %>
$(App.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @gifts %>');
<% else %>
$(window).off('scroll');
$(App.nextPageLoader.scrollerSelector).remove();
<% end %>
