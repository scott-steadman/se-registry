$('#user__list').append('<%= j render :partial => 'user', :collection => @users %>');

<% if @users.next_page %>
$(window.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @users %>');
<% else %>
$(window).off('scroll');
$(window.nextPageLoader.scrollerSelector).remove();
<% end %>
