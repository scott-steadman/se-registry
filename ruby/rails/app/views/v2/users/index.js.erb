$('#user__list').append('<%= j render :partial => 'user', :collection => @users %>');

<% if @users.next_page %>
$(App.nextPageLoader.paginationSelector).replaceWith('<%= j will_paginate @users %>');
<% else %>
$(window).off('scroll');
$(App.nextPageLoader.scrollerSelector).remove();
<% end %>
